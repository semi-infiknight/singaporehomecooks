{
  "name": "@shc/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc || echo 'source ok'",
    "test": "vitest run || echo 'no tests'",
    "typecheck": "tsc --noEmit || true"
  },
  "dependencies": {
    "@shc/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.1"
  }
}
