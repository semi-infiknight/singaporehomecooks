{
  "name": "@shc/business-rules",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "prepare": "tsc || true"
  },
  "dependencies": {
    "@shc/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "vitest": "^1.6.1",
    "typescript": "^5.5.0",
    "eslint": "^9.5.0"
  }
}
