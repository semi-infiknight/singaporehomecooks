/** Shared discover promo carousel presets (admin-managed via platform stat). */

export {
  DISCOVER_PROMOS_STAT_KEY,
  defaultDiscoverPromoConfigs,
  normalizeDiscoverPromoConfigs,
  discoverPromoConfigsToSlides,
  type DiscoverPromoConfig,
} from '@shc/utils';
