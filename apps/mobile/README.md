# Deprecated — use split apps

The unified mobile app has been replaced by:

- **Customer:** `apps/mobile-customer` — `pnpm customer:dev`
- **Cook:** `apps/mobile-cook` — `pnpm cook:dev`

Both require Medusa (`pnpm medusa:dev:admin` + `pnpm bootstrap:medusa`). No mock mode or dev role switcher.