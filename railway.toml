# Railway config for the Medusa API service ONLY.
# Web service MUST use railway.web.toml (or pnpm railway:configure-web).
# Docs: RAILWAY_DEPLOY.md, blueprint/03-railway/03-railway.md

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/medusa/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Required env vars (set in Railway dashboard — never commit):
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}
# JWT_SECRET, COOKIE_SECRET (generate random strings)
# MEDUSA_DISABLE_ADMIN=false  (admin UI at /app; Dockerfile builds with build:admin)
# MEDUSA_PUBLIC_URL=https://<service>.up.railway.app
# RAILWAY_PUBLIC_DOMAIN=<service>.up.railway.app
# Seed runs on every boot via docker-entrypoint (idempotent seed.ts, includes tiffin).
# Opt out: RAILWAY_SKIP_SEED=true
# WEB_PUBLIC_URL=https://<web-service>.up.railway.app (optional CORS auto-append)
# See RAILWAY_DEPLOY.md for full setup + web service (railway.web.toml)